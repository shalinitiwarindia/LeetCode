/**
 * @param {number[]} nums
 * @param {number} k
 * @param {number[][]} edges
 * @return {number}
 */
var maximumValueSum = function(nums, k, edges) {
    let sum = 0;
    let count = 0;
    let minLoss = Infinity;

       
    
    
 
     

    if (count % 2 === 0) {
        return sum;
    } else {
        return sum - minLoss;
 
